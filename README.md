# Default Health Files for Cloud2fi
You can create default health files, such as `PULL_REQUEST_TEMPLATE`, `CODE_OF_CONDUCT` and `CONTRIBUTING`. Default files will be used for any repository owned by this organization that does not contain its own file of that type.

While the file itself won’t appear in the file browser or Git history for each repository, it will be surfaced throughout developers’ workflows, such as when opening a new Pull Request.

For more information, see [creating a default community health file for your organization](creating a default community health file for your organization).